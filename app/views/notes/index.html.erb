<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Заметки</title>
</head>
<body>
  <h1>Все заметки</h1>
  <ul>
    <% @notes.each do |note| %>
      <li>
        <%= note.text %>
        <a href="<%= "/notes/#{note.id}/destroy" %>" rel="nofollow">Удалить</a>
      </li>
    <% end %>
  </ul>

  <a href="<%= "/notes/new" %>">Новая заметка</a>

</body>
</html>
